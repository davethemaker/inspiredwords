

<ul class="quotes">
    <% for(var i=0; i<quotes.length;i++) {%>
        <li class="quote">
            <span><%= quotes[i].author %></span>
            <span><%= quotes[i].quote %></span>
        </li>
    <% } %>
</ul>